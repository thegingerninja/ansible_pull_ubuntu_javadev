- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_cache: yes
      changed_when: False

  tasks:
    - include: tasks/sudouser.yml
      vars:
        sudo_user: paul

    - include: tasks/packages.yml
